{
  "name": "multiplayer-setup-browser-acceptance",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "(cd ../web && bun run build) && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}

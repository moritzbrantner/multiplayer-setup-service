{
  "schemaVersion": 1,
  "profile": "repository-foundation-v1",
  "tiers": {
    "fast": ["format:check", "lint", "test", "build"],
    "integration": ["test:integration"],
    "e2e": ["test:e2e"],
    "performance": ["benchmark:smoke", "profile:runtime"]
  },
  "requiredCapabilities": [
    "format:check",
    "lint",
    "test",
    "build",
    "test:integration",
    "test:e2e"
  ],
  "optionalCapabilities": ["benchmark:smoke", "profile:runtime"],
  "capabilityCommands": {
    ".": {
      "format:check": ["cargo", "fmt", "--all", "--", "--check"],
      "lint": [
        "cargo",
        "clippy",
        "--all-targets",
        "--all-features",
        "--locked",
        "--",
        "-D",
        "warnings"
      ],
      "test": ["cargo", "test", "--all-targets", "--all-features", "--locked"],
      "build": ["cargo", "build", "--locked"],
      "profile:runtime": [
        "cargo",
        "run",
        "--release",
        "--locked",
        "--bin",
        "profile-stores"
      ]
    },
    "web": {
      "test:integration": ["npm", "run", "test"],
      "benchmark:smoke": ["npm", "run", "test:perf"]
    },
    "e2e": {
      "test:e2e": ["npm", "test"]
    }
  }
}
